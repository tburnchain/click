# 글로벌 통합 제휴마케팅 데이터 추출·분석 시스템 (GAMDAP)

> **Global Affiliate Marketing Data Aggregation Platform**
> 엔터프라이즈 설계서 (System Design Document) — **v3.0**
> 작성일: 2026-07-13
> v2 변경: Perplexity 완전 배제 → 공식 API 단일 원천화, 플러그형 AI 어시스트/크롤·추적 프레임워크 추가
> **v3 변경: 자가확장 네트워크·상품 발견 엔진(§16), 지속적 대규모 수집(§17), 광고상품 분류 엔진·초지능 수학(§18), SaaS 구독·멀티테넌시(§19) 추가**

---

## 0. 이 문서를 읽는 법 (Executive Summary)

이 시스템은 **전 세계 제휴마케팅 네트워크(쿠팡 파트너스, Amazon Associates, ClickBank 등)의 상품·수수료·가격·재고 데이터를 공식 API로 수집·정규화·분류·분석하여, 어떤 상품을 홍보하는 것이 가장 수익성이 높은지를 계산·랭킹·시각화하는 의사결정 플랫폼**이다.

### 0.1 v2 핵심 결정 — 데이터 원천 (Source of Truth)

> **LLM/외부 검색을 데이터 원천으로 쓰지 않는다. 모든 거래성 데이터는 공식 제휴 API에서만 온다.**
>
> 가격·재고·정산 수수료·상품ID·딥링크는 각 네트워크의 **공식 API가 반환한 값 그대로** 저장한다. 이 값은 계약·정산의 기준이므로 100% 결정론적이어야 하며, 추정·생성된 값이 섞여선 안 된다.
>
> | Tier | 데이터 성격 | 유일한 소스 |
> |------|-----------|-----------|
> | **T1 — 거래성(정산 직결)** | 가격, 재고/가용성, 수수료율, 상품ID, 딥링크, 카테고리(원본) | **공식 제휴 API** (쿠팡 OpenAPI · Amazon PA-API v5 · ClickBank API) |
> | **T2 — 파생(자체 계산)** | 건당 기대수익, EPC, 수익성 점수, 변동 이벤트 | **자체 계산 엔진** (T1 + 규칙) |
> | **T3 — 보조(선택·플러그형)** | 카테고리 매핑 제안, 엔티티 매칭 보조, 번역, 트렌드 신호, 크롤·추적 보조 | **관리자가 등록한 AI 어시스트 플러그인(선택). 기본값=미사용** |
>
> **원칙:** *숫자는 공식 API에서, 판단은 자체 엔진에서, 보조 지능은 관리자가 켤 때만 플러그인에서.* T3는 **정확성을 훼손하지 않는 범위의 보조**로만 작동하며, T1 필드를 절대 덮어쓰지 않는다.

### 0.2 v2 핵심 결정 — 플러그형 AI 어시스트/크롤·추적 프레임워크

특정 AI(Perplexity 등)를 코드에 박지 않는다. 대신 **AI 제공자를 관리자 콘솔에서 등록·활성화·비활성화하는 확장 프레임워크**를 둔다.

- 코어는 **공식 API만으로 완결적으로 동작**한다(AI 없이도 완전 가동).
- 나중에 성능을 높이고 싶을 때, 관리자가 **AI 제공자를 등록**하고 필요한 **역량(Capability)** 만 켠다.
- 어떤 AI든(상용 API·로컬 LLM·자체 크롤 추적 에이전트) **어댑터 규격만 맞으면 플러그인으로 장착**된다 → 벤더 종속 없음, 점진적 성능 개선.

이 두 결정이 시스템의 **신뢰성(공식 API 원천)** 과 **확장성(플러그형 지능)** 을 동시에 만족시킨다.

---

## 1. 목표 · 범위 · 비목표

### 1.1 비즈니스 목표
- **G1.** 다수 제휴 네트워크의 상품/오퍼를 **단일 스키마**로 통합해 네트워크·국가·카테고리 교차 비교.
- **G2.** 각 상품의 **건당 기대수익·수익성 점수**를 계산해 자동 랭킹.
- **G3.** 가격·재고·수수료 **변동을 감지·이력화**하여 "지금 밀 상품"을 알림.
- **G4.** 운영자가 필터·차트·비교로 **10초 안에 의사결정**.
- **G5.** **AI 보조를 관리자에서 켜고 끄며 점진적으로 성능을 개선**(코어 무중단).

### 1.2 범위 (In Scope)
- 공식 API 커넥터(쿠팡·Amazon·ClickBank), 정규화·분류·엔티티 해소
- 수익성 계산·랭킹, 변동 감지, 이력 관리, 환율 반영
- REST/GraphQL API, React 대시보드, 인증·RBAC
- **플러그형 AI 어시스트/크롤·추적 프레임워크 + 관리자 등록 UI**
- 관측성(로그·메트릭·알림), 컨테이너 배포/CI

### 1.3 비목표 (Out of Scope, v1)
- 초단위 실시간 재고 스트리밍(v1은 배치 주기 갱신)
- 실제 클릭/전환 트래킹 SDK(v2 포스트백)
- 자동 콘텐츠 생성·게시
- **LLM을 거래성 데이터 원천으로 사용**(영구 비목표)

---

## 2. 핵심 설계 철학 (Design Tenets)

1. **정확성 > 규모.** 모든 수치 필드는 출처(source)·신선도(fetched_at)를 함께 저장. 거래성 값은 공식 API 원본 그대로.
2. **소스 불변성(Bronze/Silver/Gold).** 외부 원본은 절대 덮어쓰지 않는다.
3. **멱등성.** 같은 수집을 두 번 돌려도 상태 동일(자연키 UPSERT).
4. **관측 가능성.** 모든 외부 호출(공식 API·AI 플러그인)은 요청·응답·비용·지연을 로깅.
5. **컴플라이언스 우선.** 각 네트워크 약관(스크래핑 규칙·딥링크·가격표시)을 커넥터 정책으로 코드화.
6. **지능은 플러그인, 코어는 결정론.** AI 보조는 T1을 절대 변경하지 못하며, 언제든 꺼도 코어가 완전히 동작한다.

---

## 3. 전체 아키텍처

### 3.1 논리 아키텍처 (Layered)

```
┌──────────────────────────────────────────────────────────────────────┐
│  L6. Presentation   │  React 대시보드 · 관리자 콘솔 · 공개 REST/GraphQL   │
├──────────────────────────────────────────────────────────────────────┤
│  L5. Application    │  랭킹·수익성 엔진 · 검색 · 알림 · 리포트 서비스      │
├──────────────────────────────────────────────────────────────────────┤
│  L4. Domain (Gold)  │  통합 상품/오퍼 모델 · 파생 지표 · 이력             │
├──────────────────────────────────────────────────────────────────────┤
│  L3. Normalize(Silver)│ 통화/수수료/카테고리/언어 정규화 · 중복제거·엔티티해소│
│         ▲ (선택) AI 어시스트 플러그인이 "제안"을 주입 → 사람/규칙이 확정   │
├──────────────────────────────────────────────────────────────────────┤
│  L2. Ingest (Bronze)│  공식 API 커넥터 · 원본 적재(불변)                  │
├──────────────────────────────────────────────────────────────────────┤
│  L1. Orchestration  │  스케줄러 · 워커 큐 · 재시도 · 레이트리밋 · 시크릿    │
├──────────────────────────────────────────────────────────────────────┤
│  ┌─ AI Assist Plane (플러그형, 관리자 제어, 선택) ────────────────────┐  │
│  │  Provider Registry · Capability Router · Guardrail · Cost Meter    │  │
│  └───────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────┘
        ↑ 관측성(Logs/Metrics/Trace) · 보안(Auth/Secrets) 은 전 계층 횡단
```

> **AI Assist Plane**은 코어와 분리된 **옆단(side-plane)** 이다. 코어 데이터 흐름(L1→L4)은 AI 없이 완결되며, AI는 L3의 정규화 "제안"과 L4의 신호 "보강"에만, 그것도 관리자가 켰을 때만 개입한다.

### 3.2 데이터 흐름 (End-to-End)

```
[공식 제휴 API: 쿠팡/Amazon/ClickBank] ─┐
[환율 API]                              ─┤→ (L2) raw_payloads(Bronze, 불변)
                                          │
                                          ▼
                    (L3 Normalize) products / offers / prices (Silver)
                       ├─ 통화→기준통화 환산(환율 이력)
                       ├─ 수수료 파싱("3%","$7/건","567원/건")
                       ├─ 카테고리 매핑(네트워크 분류→통합 택소노미)
                       ├─ 엔티티 해소(네트워크 간 동일상품 매칭)
                       └─ [선택] AI 어시스트가 매핑/매칭 "후보" 제안(승인 필요)
                                          ▼
                    (L4 Domain) 수익성 지표 · 변동 감지 · 스냅샷 이력 (Gold)
                       └─ 수요/경쟁 신호: 공식 API 네이티브 지표(베스트랭크·판매지수·gravity)
                          + [선택] AI 트렌드 신호 보강
                                          ▼
                    (L5 App) 랭킹·필터·비교·알림  →  (L6) 대시보드·API
```

### 3.3 기술 스택 (권장)

| 영역 | 선택 | 이유 |
|------|------|------|
| 백엔드 | **Python 3.12** (수집/ETL/API) | 생태계·데이터 처리·SDK |
| API | **FastAPI** + Pydantic v2 | 타입 안전·자동 문서·비동기 |
| 큐/스케줄 | **Celery + Redis** 또는 Prefect | 재시도·레이트리밋·관측성 |
| DB | **PostgreSQL 16** | JSONB·파티셔닝·정합성 |
| 검색(선택) | OpenSearch | 전문검색·패싯 |
| 캐시 | **Redis** | 환율·핫쿼리·레이트리밋 |
| 프런트 | **React 18 + TypeScript + TanStack Query + Recharts/ECharts** | 데이터 집약 UI |
| 인프라 | Docker(dev) / Kubernetes(prod 선택) | 이식성·확장 |
| 관측성 | OpenTelemetry + Prometheus + Grafana + Sentry | 로그·메트릭·트레이스·에러 |

> 소규모 v1은 **PostgreSQL + FastAPI + Celery/Redis + React** 로 충분. OpenSearch·K8s는 규모 확대 시 도입.

---

## 4. 데이터베이스 설계 (전문가 관점)

### 4.1 설계 원칙
- **3단 스키마:** `bronze`(원본 불변) · `core`(정규화·통합) · `analytics`(파생·집계).
- **자연키 UPSERT:** `(network_id, external_product_id)` 를 오퍼 자연키로 → 재수집 안전.
- **이력 분리:** 현재값은 `offers`, 변화는 `price_history`/`offer_snapshots`(SCD Type 2 개념).
- **화폐 원자성:** `amount NUMERIC(18,4) + currency CHAR(3)` 쌍. 부동소수 금지. 기준통화 환산은 파생.
- **파티셔닝:** `price_history`, `api_call_logs` 월 단위 RANGE 파티션.
- **소프트 삭제:** 물리 삭제 금지 → `is_active`, `delisted_at`.

### 4.2 ERD (개념도)

```
countries ──< networks ──< network_categories
   │             │  \
categories(택소노미)  offers >── products >── product_translations
   │             │      │  \
merchants ───────┘      │   └──< price_history
exchange_rates          │   └──< commission_rules
ingestion_jobs ──< api_call_logs   └──< offer_snapshots
                        └──< profitability_scores (analytics)
ai_providers ──< ai_capabilities ──< ai_suggestions   (AI Assist Plane)
users ──< saved_views / alerts / audit_logs
```

### 4.3 핵심 테이블 DDL (PostgreSQL) — 코어

```sql
CREATE SCHEMA IF NOT EXISTS core;
CREATE SCHEMA IF NOT EXISTS bronze;
CREATE SCHEMA IF NOT EXISTS analytics;

-- 참조
CREATE TABLE core.countries (
  id SERIAL PRIMARY KEY, iso_code CHAR(2) UNIQUE NOT NULL,
  name TEXT NOT NULL, default_currency CHAR(3) NOT NULL);

CREATE TABLE core.networks (
  id SERIAL PRIMARY KEY, code TEXT UNIQUE NOT NULL,        -- 'coupang_partners','amazon_assoc','clickbank'
  display_name TEXT NOT NULL, home_country_id INT REFERENCES core.countries(id),
  data_source TEXT NOT NULL DEFAULT 'official_api',        -- v2: 항상 official_api
  api_base_url TEXT, terms_url TEXT, is_active BOOLEAN DEFAULT TRUE,
  meta JSONB DEFAULT '{}'::jsonb);

CREATE TABLE core.categories (
  id SERIAL PRIMARY KEY, parent_id INT REFERENCES core.categories(id),
  slug TEXT UNIQUE NOT NULL, name_ko TEXT, name_en TEXT, path LTREE,
  UNIQUE (parent_id, slug));

CREATE TABLE core.network_categories (
  id SERIAL PRIMARY KEY, network_id INT REFERENCES core.networks(id),
  raw_name TEXT NOT NULL, category_id INT REFERENCES core.categories(id),
  mapping_confidence NUMERIC(3,2) DEFAULT 1.0,
  mapped_by TEXT DEFAULT 'rule',                           -- 'rule'|'ai_suggested'|'human'
  UNIQUE (network_id, raw_name));

-- 상품(네트워크 간 통합)
CREATE TABLE core.products (
  id BIGSERIAL PRIMARY KEY, canonical_name TEXT NOT NULL, brand TEXT, gtin TEXT,
  category_id INT REFERENCES core.categories(id), primary_image_url TEXT,
  created_at TIMESTAMPTZ DEFAULT now(), updated_at TIMESTAMPTZ DEFAULT now());
CREATE INDEX idx_products_gtin ON core.products(gtin) WHERE gtin IS NOT NULL;
CREATE INDEX idx_products_name_trgm ON core.products USING gin (canonical_name gin_trgm_ops);

CREATE TABLE core.product_translations (
  product_id BIGINT REFERENCES core.products(id), lang CHAR(2),
  name TEXT, description TEXT,
  source TEXT DEFAULT 'official', confidence NUMERIC(3,2) DEFAULT 1.0,
  PRIMARY KEY (product_id, lang));

-- 오퍼(네트워크별 조건) — 시스템의 심장
CREATE TABLE core.offers (
  id BIGSERIAL PRIMARY KEY,
  network_id INT NOT NULL REFERENCES core.networks(id),
  external_product_id TEXT NOT NULL,
  product_id BIGINT REFERENCES core.products(id),
  title TEXT NOT NULL, landing_url TEXT, thumbnail_url TEXT,
  -- 가격
  price_amount NUMERIC(18,4), price_currency CHAR(3),
  price_krw NUMERIC(18,4), price_usd NUMERIC(18,4),        -- 파생
  -- 과금/수수료
  billing_type TEXT CHECK (billing_type IN ('CPS','CPC','CPM','CPA','CPL')),
  commission_kind TEXT CHECK (commission_kind IN ('percent','fixed')),
  commission_rate NUMERIC(6,4), commission_fixed_amount NUMERIC(18,4), commission_currency CHAR(3),
  -- 재고/가용성
  stock_status TEXT CHECK (stock_status IN ('in_stock','low','out_of_stock','digital_unlimited','unknown')),
  stock_quantity INT, is_active BOOLEAN DEFAULT TRUE, delisted_at TIMESTAMPTZ,
  -- 네이티브 인기 지표(공식 API가 주는 것)
  native_rank INT,                                          -- 쿠팡 베스트랭크/Amazon SalesRank
  native_metric_json JSONB DEFAULT '{}'::jsonb,             -- ClickBank gravity 등
  -- 거버넌스
  data_source TEXT NOT NULL DEFAULT 'official_api',
  fetched_at TIMESTAMPTZ NOT NULL, raw_ref BIGINT,
  created_at TIMESTAMPTZ DEFAULT now(), updated_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE (network_id, external_product_id));
CREATE INDEX idx_offers_network ON core.offers(network_id);
CREATE INDEX idx_offers_product ON core.offers(product_id);
CREATE INDEX idx_offers_active  ON core.offers(is_active) WHERE is_active;
CREATE INDEX idx_offers_fetched ON core.offers(fetched_at);

-- 수수료 정책(메타)
CREATE TABLE core.commission_rules (
  id SERIAL PRIMARY KEY, network_id INT REFERENCES core.networks(id),
  category_id INT REFERENCES core.categories(id), billing_type TEXT,
  commission_kind TEXT, rate NUMERIC(6,4), fixed_amount NUMERIC(18,4), currency CHAR(3),
  country_id INT REFERENCES core.countries(id),
  effective_from DATE, effective_to DATE, data_source TEXT DEFAULT 'official_api');

-- 이력(월 파티션)
CREATE TABLE core.price_history (
  id BIGSERIAL, offer_id BIGINT NOT NULL REFERENCES core.offers(id),
  observed_at TIMESTAMPTZ NOT NULL,
  price_amount NUMERIC(18,4), price_currency CHAR(3),
  commission_rate NUMERIC(6,4), commission_fixed_amount NUMERIC(18,4),
  stock_status TEXT, stock_quantity INT,
  PRIMARY KEY (id, observed_at)) PARTITION BY RANGE (observed_at);

CREATE TABLE core.exchange_rates (
  base_currency CHAR(3), quote_currency CHAR(3), rate NUMERIC(18,8) NOT NULL,
  as_of DATE NOT NULL, source TEXT,
  PRIMARY KEY (base_currency, quote_currency, as_of));

-- 원본(Bronze) 불변
CREATE TABLE bronze.raw_payloads (
  id BIGSERIAL PRIMARY KEY, network_code TEXT, source TEXT,
  request JSONB, response JSONB, http_status INT, cost_usd NUMERIC(10,6),
  fetched_at TIMESTAMPTZ DEFAULT now());

-- 파생(Analytics)
CREATE TABLE analytics.profitability_scores (
  offer_id BIGINT PRIMARY KEY REFERENCES core.offers(id),
  expected_earning_per_sale NUMERIC(18,4), expected_epc NUMERIC(18,6),
  demand_index NUMERIC(6,3), competition_index NUMERIC(6,3),
  freshness_factor NUMERIC(4,3), profitability_score NUMERIC(7,3),
  computed_at TIMESTAMPTZ DEFAULT now());

-- 운영/거버넌스
CREATE TABLE core.ingestion_jobs (
  id BIGSERIAL PRIMARY KEY, network_code TEXT, job_type TEXT, status TEXT,
  params JSONB, rows_upserted INT, rows_changed INT, cost_usd NUMERIC(10,6),
  started_at TIMESTAMPTZ, finished_at TIMESTAMPTZ, error TEXT);
```

### 4.4 AI Assist Plane 테이블 (v2 신규)

```sql
-- 관리자가 등록하는 AI 제공자(상용 API/로컬 LLM/크롤·추적 에이전트)
CREATE TABLE core.ai_providers (
  id SERIAL PRIMARY KEY,
  code TEXT UNIQUE NOT NULL,               -- 'openai','anthropic','local_llm','crawl_tracker_x'
  display_name TEXT NOT NULL,
  adapter TEXT NOT NULL,                   -- 어댑터 구현 식별자(플러그인 클래스)
  base_url TEXT,
  secret_ref TEXT,                         -- 시크릿매니저 키 참조(원문 키 저장 금지)
  is_enabled BOOLEAN DEFAULT FALSE,        -- 기본 비활성(코어는 AI 없이 동작)
  monthly_budget_usd NUMERIC(10,2),        -- 비용 상한
  rate_limit_per_min INT,
  config JSONB DEFAULT '{}'::jsonb,        -- 모델명·온도 등
  created_at TIMESTAMPTZ DEFAULT now(), updated_at TIMESTAMPTZ DEFAULT now());

-- 제공자가 어떤 역량을 담당하는지(역량 슬롯 라우팅)
CREATE TABLE core.ai_capabilities (
  id SERIAL PRIMARY KEY,
  provider_id INT REFERENCES core.ai_providers(id),
  capability TEXT NOT NULL,                -- 'category_mapping'|'entity_matching'|'translation'
                                           -- |'trend_signal'|'crawl_assist'|'change_tracking'
  is_enabled BOOLEAN DEFAULT FALSE,
  priority INT DEFAULT 100,                -- 같은 역량 다중 제공자 시 우선순위/폴백
  params JSONB DEFAULT '{}'::jsonb,
  UNIQUE (provider_id, capability));

-- AI가 낸 "제안"은 코어에 직접 쓰지 않고 별도 저장 → 규칙/사람이 승인해야 반영
CREATE TABLE core.ai_suggestions (
  id BIGSERIAL PRIMARY KEY,
  provider_id INT REFERENCES core.ai_providers(id),
  capability TEXT NOT NULL,
  target_type TEXT,                        -- 'network_category'|'product_match'|'offer_signal'
  target_id TEXT,
  suggestion JSONB NOT NULL,               -- 제안 내용(예: 매핑 후보, 매칭 후보, 트렌드 점수)
  confidence NUMERIC(3,2),
  status TEXT DEFAULT 'pending',           -- 'pending'|'accepted'|'rejected'|'auto_applied'
  reviewed_by INT, reviewed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now());
CREATE INDEX idx_ai_sugg_status ON core.ai_suggestions(status);

-- AI/공식 API 호출 관측(비용·지연·성공률)
CREATE TABLE core.api_call_logs (
  id BIGSERIAL, provider TEXT, kind TEXT,  -- 'official_api'|'ai_assist'
  capability TEXT, request JSONB, http_status INT,
  latency_ms INT, cost_usd NUMERIC(10,6), called_at TIMESTAMPTZ NOT NULL,
  PRIMARY KEY (id, called_at)) PARTITION BY RANGE (called_at);
```

### 4.5 왜 `offers`와 `products`를 분리하나
동일 실물 상품이 쿠팡·Amazon·ClickBank에 각기 다른 가격·수수료로 존재한다. `products`=실물 1개, `offers`=네트워크별 조건 N개. 이 분리가 있어야 "같은 상품, 어느 네트워크가 더 남나?" 비교와 엔티티 해소가 가능하다.

### 4.6 AI 제안의 격리 (핵심 안전장치)
`ai_suggestions`는 **코어 테이블과 분리**된다. AI는 절대 `offers`/`products`를 직접 수정하지 않고 **제안만** 남긴다. 규칙 임계 통과 또는 사람 승인 시에만 `network_categories.mapped_by='ai_suggested'` 등으로 반영된다. → **AI를 켜도 코어 정확성이 오염되지 않는다.**

---

## 5. 데이터 정규화 엔진 (Silver Layer)

### 5.1 수수료 파서
| 원본 | 결과 |
|------|------|
| `"3%"` | percent, 0.03 |
| `"5~10%"` | percent, 0.075(평균), meta.range=[.05,.10] |
| `"567원/건"` | fixed, 567, KRW |
| `"$7/건"` | fixed, 7, USD |
| `"200원/클릭"` | fixed, 200, KRW, CPC |

정규식+통화/단위 사전. 실패 시 원본 보존 + `needs_review=true`.

### 5.2 통화·환율
원통화 저장 + 기준통화(KRW/USD) 파생. 환율 매일 1회 적재(한국은행/ECB 오픈API), Redis 24h 캐시. `price_krw = amount × rate(cur→KRW, as_of)`.

### 5.3 카테고리 매핑
1차 **규칙 사전**(가전=electronics …). 미매핑은 리뷰 큐. **(선택)** AI `category_mapping` 역량이 켜져 있으면 후보를 `ai_suggestions`에 제안 → 승인 시 사전에 학습.

### 5.4 엔티티 해소
- 강한 키: GTIN/모델명 일치 → 즉시 병합.
- 약한 키: 이름 트라이그램(pg_trgm)+브랜드+가격 근접 → 후보.
- 애매 구간은 리뷰 큐. **(선택)** AI `entity_matching` 역량이 후보 점수를 제안(자동 병합 아님).

---

## 6. 수집·오케스트레이션 (Bronze/L2 + L1)

### 6.1 커넥터 아키텍처 (공식 API 전용)

```python
class BaseConnector(Protocol):
    code: str
    def fetch_offers(self, since, category) -> Iterable[RawOffer]: ...
    def fetch_commission_policy(self) -> Iterable[RawRule]: ...
    def rate_limit(self) -> RateLimit: ...
    def terms_policy(self) -> TermsPolicy: ...
```

- **CoupangPartnersConnector** — 쿠팡 파트너스 OpenAPI(HMAC 서명): 상품검색·베스트·딥링크·가격. `native_rank`=베스트 순위.
- **AmazonPaapiConnector** — PA-API v5: 가격·이미지·가용성(Availability)·SalesRank·수수료 카테고리. (요청 스로틀·판매 실적 요건 준수)
- **ClickBankConnector** — Marketplace API: 제품·`gravity`·평균수익·초기/재청구 커미션. 디지털=`digital_unlimited`.

> **약관 준수:** 각 커넥터는 `terms_policy()`로 허용 범위를 코드화. 공식 API/피드가 주는 필드만 사용, 임의 스크래핑 금지.

### 6.2 스케줄 전략

| 데이터 | 주기 | 방식 |
|--------|------|------|
| 가격·재고(핫 Top N) | 1~6시간 | 공식 API 증분 |
| 전체 카탈로그 | 매일 1회 | 공식 API 풀/증분 |
| 수수료 정책 | 주 1회 | 공식 API |
| 환율 | 매일 1회 | 환율 API |
| (선택) AI 트렌드/매핑 배치 | 주 1회 | AI 플러그인(켜진 경우) |

### 6.3 신뢰성 패턴
- 레이트리밋(토큰버킷, 429 지수백오프), 멱등 작업만 자동 재시도.
- 행 단위 실패 격리(+`needs_review`), 배치 전체 중단 방지.
- 멱등 UPSERT + 변경분만 `price_history`. 변동 시 이벤트 발행(알림).

---

## 7. AI 어시스트 / 크롤·추적 프레임워크 (v2 신규, 플러그형)

### 7.1 목적
코어를 건드리지 않고 **관리자가 AI를 장착해 성능(정확도·커버리지·발견력)을 점진 개선**한다. 특히 **크롤·추적 보조 AI**(변경 감지, 적응형 파싱, 비정형 피드 구조화)를 붙여 데이터 신선도·품질을 높인다.

### 7.2 역량(Capability) 슬롯
| capability | 하는 일 | 코어에 미치는 방식 |
|-----------|--------|------------------|
| `category_mapping` | 미매핑 원본 카테고리 → 통합 택소노미 후보 | `ai_suggestions` → 승인 시 반영 |
| `entity_matching` | 네트워크 간 동일상품 후보 점수화 | 제안만, 자동 병합 금지 |
| `translation` | 상품명/설명 다국어 | `product_translations`(confidence<1) |
| `trend_signal` | 수요/트렌드 신호 산출 | `demand_index` **보강**(대체 아님) |
| `crawl_assist` | 공식 피드가 비정형일 때 파싱·필드 추출 보조 | Bronze→Silver 변환 보조 |
| `change_tracking` | 변경 우선순위 예측(어떤 오퍼를 더 자주 볼지) | 스케줄러 큐 우선순위 힌트 |

### 7.3 어댑터 규격 (벤더 중립)

```python
class AIAssistAdapter(Protocol):
    code: str
    def supports(self) -> set[str]: ...          # 제공 capability 집합
    def run(self, capability: str, payload: dict) -> AISuggestion: ...
    def health(self) -> HealthStatus: ...
    def unit_cost(self) -> float: ...
```
- 어떤 제공자든(상용 LLM API·로컬 모델·자체 크롤 추적 서비스) 이 규격만 구현하면 등록 가능.
- **Capability Router**: `ai_capabilities`의 `priority`로 라우팅·폴백. 한 제공자 다운 시 다음 우선순위로.

### 7.4 가드레일 (안전장치)
- **T1 불변:** AI는 가격·재고·수수료·상품ID를 **절대 수정 불가**(쓰기 권한 없음).
- **제안 격리:** 모든 산출은 `ai_suggestions`(status=pending)로만. `auto_apply`는 관리자가 역량별로 임계·허용을 명시할 때만.
- **비용 상한:** 제공자별 `monthly_budget_usd` 초과 시 자동 비활성 + 알림.
- **관측:** 모든 호출 `api_call_logs`(비용·지연·성공률).
- **기본 OFF:** 신규 설치 시 AI 전부 비활성 → 코어만으로 완전 가동.

### 7.5 관리자 등록 흐름
1. 관리자 콘솔 → "AI 제공자 추가" → 어댑터 선택·시크릿 등록·모델/예산 설정.
2. 역량 토글(예: `category_mapping` ON, `trend_signal` ON).
3. 헬스체크 통과 시 활성. 제안이 리뷰 큐에 쌓이고, 승인률·정확도·비용이 대시보드에 집계.
4. 성능 지표를 보고 역량별로 자동적용 임계를 조정 → **점진적 성능 개선 루프**.

---

## 8. 수익성 계산 엔진 (초지능적 수학) — L4/Analytics

### 8.1 왜 수수료율만으로 랭킹하면 안 되나
15%×5,000원=750원 vs 3%×130만원=39,000원. 랭킹은 **건당 기대수익 × 전환 가능성 × 수요**의 함수여야 한다.

### 8.2 정의
**건당 기대수익(기준통화):**
```
E_sale = price_krw × commission_rate        (percent)
E_sale = fixed_amount_krw                    (fixed)
```
**클릭당 기대수익(EPC):** `EPC ≈ E_sale × CVR_est`
- `CVR_est`: 카테고리·가격대 사전값. 실측(v2)이 쌓이면 베이지안 업데이트 `CVR=(α+전환)/(α+β+클릭)` → 데이터 적을수록 사전값 수렴(과적합 방지).

**수요 지수(0~1):** **공식 API 네이티브 지표**로 산출 — 쿠팡 베스트랭크, Amazon SalesRank, ClickBank gravity를 카테고리 내 정규화. **(선택)** AI `trend_signal`이 켜지면 가중 결합으로 보강.
**경쟁 지수(0~1):** 동일 상품을 파는 네트워크 수·오퍼 밀도.

### 8.3 최종 수익성 점수 (0~100, 곱셈적)
```
raw   = norm(EPC)^w1 × Demand^w2 × (1−Competition)^w3      (w1+w2+w3=1, 예: .5/.3/.2)
Score = 100 × raw × freshness_factor
freshness_factor = exp(−Δt / τ)                           (데이터 나이 지수 감쇠)
```
- `norm(EPC)`: 카테고리 내 분위수 정규화(이상치 완화).
- 곱셈적 결합 → "수수료 높지만 수요 0"인 함정 상품 자동 배제.
- v2에선 모든 T1이 공식 API라 `source_confidence`는 상수(1.0). 대신 **신선도** 가 주된 감쇠 요인.

### 8.4 변동·기회 감지
- 가격 급락+수수료 유지 → "지금 밀 상품".
- 수수료 인상(기획전) → 한시 고수익 알림.
- 재고 소진 임박 → 홍보 중단 권고.
- `price_history` 시계열 이동평균 대비 z-score 임계 초과 시 이벤트.

---

## 9. 백엔드 API 설계

### 9.1 REST (FastAPI, OpenAPI 자동 문서)
```
# 조회
GET /api/v1/offers?network=&category=&country=&billing_type=&min_price=&max_price=&sort=score|epc|commission&page=&size=
GET /api/v1/offers/{id}
GET /api/v1/products/{id}/offers            # 동일 상품 네트워크별 비교
GET /api/v1/rankings?category=&country=&limit=50
GET /api/v1/compare?product_id=
GET /api/v1/offers/{id}/history?field=price|commission|stock&from=&to=
# 분석
GET /api/v1/analytics/avg-commission?group_by=network,category,country
GET /api/v1/analytics/opportunities
GET /api/v1/analytics/coverage             # 네트워크별 신선도/커버리지
# 운영(admin)
POST /api/v1/ingest/{network}/run
GET  /api/v1/jobs?status=
GET  /api/v1/costs?month=
# AI Assist(admin)
GET    /api/v1/ai/providers
POST   /api/v1/ai/providers                # 등록
PATCH  /api/v1/ai/providers/{id}           # 활성/예산/모델
POST   /api/v1/ai/providers/{id}/capabilities   # 역량 토글
GET    /api/v1/ai/suggestions?status=pending
POST   /api/v1/ai/suggestions/{id}/accept  # 승인→코어 반영
POST   /api/v1/ai/suggestions/{id}/reject
# 사용자
POST /api/v1/saved-views
POST /api/v1/alerts
```

### 9.2 응답 규약
- 목록: `{data, page, size, total, facets}`.
- 금액: `{amount, currency, krw, usd}` 4쌍.
- 각 오퍼에 `data_source`, `fetched_at`(신선도), `native_rank`.
- 에러: RFC 7807 `application/problem+json`.

### 9.3 성능
- 랭킹은 `analytics.profitability_scores` 사전계산 + 머티리얼라이즈드 뷰.
- 핫 쿼리 Redis 캐시, 패싯 OpenSearch(선택), 커서 페이지네이션.

---

## 10. 프런트엔드 UI/UX 설계 (사용자 관점 디테일)

### 10.1 UX 원칙
- **10초 의사결정:** 첫 화면에서 오늘 밀 상품 Top 즉시.
- **신선도 투명성:** 각 값의 마지막 갱신 시각을 뱃지로("2시간 전"·오래되면 주황).
- **비교 중심:** "이 상품, 어느 네트워크가 더 남나?" 한 클릭.
- **점진 노출:** 목록=요약 · 호버=핵심지표 · 클릭=전체+이력.

### 10.2 정보 구조
```
┌ 사이드바 ──────────┬ 메인 ──────────────────────────────────────────┐
│ • 대시보드          │  [KPI 스트립] 총오퍼·활성네트워크·평균EPC·오늘기회 │
│ • 상품 탐색         │  [필터바] 국가▾ 네트워크▾ 카테고리▾ 과금▾ 가격레인지│
│ • 랭킹(수익성)      │  [테이블/카드 토글]                              │
│ • 네트워크 비교      │   썸네일·상품명·네트워크·가격·수수료·건당수익·점수·신선도│
│ • 기회 알림         │                                                 │
│ • 이력/트렌드       │                                                 │
│ • ⚙ 관리자          │   ├ 수집/작업 · 비용 · 데이터품질                 │
│   └ AI 제공자 관리   │   └ AI 제공자·역량·제안 리뷰 큐                   │
│ • 설정              │                                                 │
└────────────────────┴────────────────────────────────────────────────┘
```

### 10.3 핵심 컴포넌트
**(1) 오퍼 테이블:** `썸네일 | 상품명 | 네트워크(로고) | 국가 | 카테고리 | 가격(현지/KRW) | 과금 | 수수료 | 건당수익 | 수익성점수(컬러바) | 신선도 | 네이티브랭크`. 정렬·다중필터·URL 동기화, 행 호버 시 30일 가격 스파크라인.
**(2) 네트워크 비교:** 동일 `product_id` 오퍼 나란히, **승자 하이라이트** + "왜 더 남는가" 한 줄.
**(3) 랭킹:** 카테고리·국가별 Top 50 카드(점수·건당수익·수요/경쟁 게이지·기회 태그).
**(4) 기회 알림:** 변동 피드("🔥 기획전 수수료 3%→15%", "⚠️ 재고 25→5"), 구독/스누즈, 이메일/슬랙(발송은 사용자 승인).
**(5) 이력/트렌드:** 가격·수수료·재고 3중 시계열 + 변동 마커.
**(6) 관리자 — 운영:** 공식 API 호출 수·성공률·비용, 수집 타임라인, `needs_review` 큐.
**(7) 관리자 — AI 제공자 관리(v2 신규):**
- 제공자 목록(활성/예산/이번달 비용/성공률), "제공자 추가" 마법사(어댑터·시크릿·모델·예산).
- **역량 매트릭스**: 행=역량, 열=제공자, 셀=토글+우선순위.
- **제안 리뷰 큐**: AI 제안(매핑/매칭/트렌드) 승인·반려, 승인율·정확도 추이.
- **성능 개선 패널**: 역량별 "AI ON vs OFF" 정확도/커버리지 비교 → 자동적용 임계 조정.

### 10.4 상태·엣지케이스
- 로딩=스켈레톤, 빈 상태=필터 완화 제안, 오래된 데이터=마지막 수집 시각 배너+수동 새로고침(admin), 부분 실패=실패 네트워크만 배지.

### 10.5 디자인 시스템
- 라이트/다크, 반응형(테이블→모바일 카드), 네트워크 로고·국기, 통화 로케일(₩/$/¥).
- 접근성: 색+아이콘 이중 인코딩, 키보드 내비, ARIA.

---

## 11. 보안 · 컴플라이언스

- **시크릿:** 공식 API 키·AI 키·DB 자격증명은 시크릿매니저(Vault/SSM). `ai_providers.secret_ref`는 참조만, 원문 저장 금지.
- **인증/권한:** OAuth2/OIDC + RBAC(viewer/analyst/admin). **AI 제공자 등록·제안 승인·비용 열람은 admin 전용.**
- **약관 준수:** 커넥터 `terms_policy` 코드화, 공식 API/피드만 사용. 크롤·추적 AI도 약관 허용 범위에서만 동작(금지 네트워크 차단).
- **AI 격리:** AI는 T1 쓰기 권한 없음. 제안만, 승인 게이트 필수.
- **감사 로그:** 누가 언제 무엇을 조회/트리거/승인했는지 `audit_logs`.
- **비용 가드레일:** 제공자별 예산 상한 초과 시 자동 비활성.

---

## 12. 관측성 · 운영
- 구조화 JSON 로그(모든 외부 호출 req/res/cost/latency).
- 메트릭: 수집 성공률, 신선도 분포, 커넥터 지연, AI 역량별 비용/정확도, 랭킹 재계산 시간.
- 트레이스: OTel로 수집→정규화→점수 추적.
- 알림: 수집 실패율↑, 신선도 임계 초과, AI 예산 80/100%, `needs_review` 큐 급증.
- 데이터 품질 대시보드: 미매핑 카테고리, 리뷰 큐 길이, 엔티티 병합률, AI 제안 승인율.

---

## 13. 개발 로드맵 (마일스톤)

| 단계 | 기간(예상) | 산출물 |
|------|-----------|--------|
| **M0. 기반** | 1주 | DB 스키마·마이그레이션, 뼈대(FastAPI/Celery/Redis/Postgres), 시크릿 |
| **M1. 쿠팡 커넥터** | 1.5주 | CoupangPartnersConnector, Bronze 적재, 멱등 UPSERT, price_history |
| **M2. 정규화 엔진** | 1주 | 수수료 파서·환율·카테고리 매핑·통화 파생 |
| **M3. 수익성 엔진** | 1주 | E_sale/EPC/Score, analytics 사전계산, 변동 감지 |
| **M4. API + 최소 UI** | 1.5주 | REST(offers/rankings/compare), React 테이블·필터·랭킹·비교 |
| **M5. 확장 커넥터** | 2주 | Amazon PA-API, ClickBank |
| **M6. AI Assist 프레임워크** | 1.5주 | Provider Registry·Capability Router·가드레일·제안 큐·관리자 UI (어댑터 1종 레퍼런스) |
| **M7. 알림·이력·운영콘솔** | 1.5주 | 기회 알림, 트렌드 차트, 비용/품질/AI 대시보드 |
| **M8. 광고상품 분류 엔진(§18)** | 2주 | 특징벡터·계층분류(신뢰도)·기회 사분면·세그먼트 뱃지·기회지도 UI (임베딩 폴백 포함) |
| **M9. 자가확장 발견(§16)** | 2주 | discovery_sources·network_candidates·검증·후보스코어·UCB 탐사정책·승인 온보딩 |
| **M10. 지속 수집 스케일(§17)** | 1.5주 | Freshness 계층·샤딩·증분·대규모 dedup·서킷브레이커·수집→분류 자동연쇄 |
| **M11. SaaS 구독·멀티테넌시(§19)** | 2주 | tenants/plans/subscriptions·RLS·엔타이틀먼트 게이트·사용량 계량·Stripe 웹훅·플랜 게이팅 |
| **M12. 하드닝** | 1주 | 관측성·보안·부하/정확성 테스트·문서 |

> **MVP 경계(M0~M4):** 쿠팡 단일 네트워크 + 정확한 DB + 수익성 랭킹 + 대시보드 = **AI 없이 동작하는 완결 시스템**. AI는 M6에서 플러그형 보조로 합류(코어 무중단).
> **제품화 순서:** 분류 엔진(§18, 구독 가치의 핵심) → 자가확장 발견(§16) → 지속수집 스케일(§17) → SaaS 과금(§19). 분류가 구독료의 핵심 가치이므로 발견보다 먼저 완성한다.

---

## 14. 비용 개요
- **공식 API:** 대부분 무료/저비용(파트너 계정 기준). 데이터 규모는 여기서 확보 → **AI 없이도 대량·정확**.
- **AI Assist(선택):** 관리자가 켠 역량·제공자에 한해 과금. 제공자별 월 예산 상한으로 통제. 배치화·캐시로 최소화.
- **인프라:** 소규모는 단일 VM(Postgres+Redis+앱)에서 시작, K8s는 규모 확대 시.

---

## 15. 리스크 · 완화

| 리스크 | 영향 | 완화 |
|--------|------|------|
| 공식 API 스로틀/쿼터(특히 Amazon PA-API 판매요건) | 커버리지 공백 | 증분 수집·백오프·핫상품 우선, 요건 유지 모니터 |
| AI 제안 오류로 매핑/매칭 오염 | 비교 왜곡 | 제안 격리·승인 게이트·자동적용 임계 보수적 |
| 네트워크 약관 위반(크롤 AI 오용) | 계정 정지 | terms_policy·금지 네트워크 차단, 공식 API/허용 피드만 |
| 환율/통화 오류 | 수익 계산 왜곡 | NUMERIC·환율 이력·단위 테스트 |
| AI 비용 폭증 | 예산 초과 | 제공자별 상한·자동 비활성·캐시·배치 |
| 카테고리/엔티티 오매핑 | 비교 왜곡 | 규칙 우선·리뷰 큐·Human-in-the-loop |
| 자가확장이 저품질 네트워크 남발 | 카탈로그 오염·비용 | 후보 곱셈 게이트(원천·약관 없으면 0점)·UCB 예산 통제·승인 게이트 |
| 분류 오류(잘못된 세그먼트) | 구독자 오판 | 신뢰도 임계·룰 보정·저신뢰는 군집큐·중심점 EMA 학습 |
| 임베딩 AI 의존 | AI 중단 시 분류 저하 | TF-IDF 폴백으로 코어 분류 지속(정확도만 하락) |
| 멀티테넌트 데이터 유출 | 신뢰·법적 리스크 | RLS 강제·tenant_id 필수·크로스테넌트 차단·감사로그 |
| 신선도 차등 불만(하위 플랜) | 이탈 | 플랜별 가치 명확화·업그레이드 유도·공정 사용 정책 |

---

## 16. 자가확장 네트워크·상품 발견 엔진 (Self-Expanding Discovery)

시스템은 **스스로 새 제휴 네트워크를 찾아 후보로 등록하고, 검증·승인 후 커넥터로 편입**한다. 단, **§0의 원칙은 불변** — 발견은 "후보 생성"까지만 하고, **실제 데이터 원천이 되려면 공식 API/승인된 피드를 갖춘 커넥터로 승격**되어야 한다. 발견이 곧 데이터 신뢰를 의미하지 않는다.

### 16.1 발견 파이프라인 (Discover → Vet → Onboard → Grow)

```
[발견 소스] → network_candidates(pending)
   ├─ 제휴 네트워크 디렉토리/애그리게이터(공개 목록)
   ├─ 머천트 "제휴하기(Affiliate Program)" 페이지 시그니처 탐지
   ├─ (선택) AI 'discovery/crawl_assist' 역량이 후보 URL·프로그램 제안
   └─ 운영자 수동 제보
        ▼ (자동 검증 Vetting)
   ├─ 공식 API/상품피드 존재 여부  ├─ 약관(스크래핑·딥링크 규칙)  ├─ 수수료 실현성
   ├─ 카테고리 적합도             └─ 중복(이미 등록된 네트워크?)
        ▼ (스코어링 → 승인 게이트)
   candidate_score ≥ θ → 자동 승격 후보 / 그 외 → 운영자 리뷰 큐
        ▼ (Onboard)
   커넥터 프로비저닝(어댑터 매핑·시크릿 등록·레이트리밋) → networks(active)
        ▼ (Grow)
   §17 지속 수집 + §18 분류가 자동 개시, §16.3 탐사정책이 크롤 예산 배분
```

### 16.2 발견/검증 테이블

```sql
CREATE TABLE core.discovery_sources (
  id SERIAL PRIMARY KEY, kind TEXT,          -- 'directory'|'merchant_page'|'ai'|'manual'
  url TEXT, trust NUMERIC(3,2) DEFAULT 0.5,   -- 소스 신뢰도(후보 스코어 가중)
  is_enabled BOOLEAN DEFAULT TRUE, last_scanned_at TIMESTAMPTZ);

CREATE TABLE core.network_candidates (
  id BIGSERIAL PRIMARY KEY,
  name TEXT NOT NULL, home_url TEXT, program_url TEXT,
  country_iso CHAR(2), discovered_by INT REFERENCES core.discovery_sources(id),
  -- 검증 시그널
  has_official_api BOOLEAN, api_doc_url TEXT,
  has_product_feed BOOLEAN, feed_url TEXT,
  terms_scrape_allowed BOOLEAN, deeplink_policy TEXT,
  est_commission_hint TEXT, category_fit NUMERIC(3,2),
  duplicate_of INT REFERENCES core.networks(id),
  candidate_score NUMERIC(6,3),               -- §16.4 공식
  status TEXT DEFAULT 'pending',              -- 'pending'|'vetting'|'approved'|'onboarded'|'rejected'
  reviewed_by INT, reviewed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now());
CREATE INDEX idx_cand_status ON core.network_candidates(status);
CREATE INDEX idx_cand_score  ON core.network_candidates(candidate_score DESC);
```

### 16.3 탐사 정책 — 어디를 더 파고들 것인가 (Multi-Armed Bandit)
"어느 (네트워크 × 카테고리)를 더 크롤·발견에 투자할지"를 **UCB1 밴딧**으로 학습해 크롤 예산을 최적 배분한다. 알려진 고수익 니치 착취(exploitation)와 새 네트워크 탐험(exploration)의 균형:

```
UCB(arm) = R̄(arm) + √( 2·ln N / n(arm) )
  R̄(arm)  : 해당 arm에서 발견한 '수익가치/크롤비용'의 평균 보상(§18 Opportunity 기반)
  n(arm)   : arm 크롤 횟수,  N : 전체 크롤 횟수
매 사이클: UCB 상위 arm에 크롤 슬롯 배분 → 보상 관측 → 갱신
```
→ 신규 네트워크는 `n(arm)`이 작아 탐험 보너스가 커져 **자동으로 초기 탐사**되고, 성과가 낮으면 자연히 예산이 빠진다. **자가확장이 무한정 비용을 태우지 않도록 수학적으로 통제**된다.

### 16.4 후보 스코어 (승격 우선순위)
```
candidate_score = 100 · source_trust
                 · 𝟙[has_official_api ∨ has_product_feed]     (원천 확보 가능성, 없으면 0)
                 · 𝟙[terms_scrape_allowed ∨ has_official_api] (약관 적합, 위반이면 0)
                 · sigmoid(w1·category_fit + w2·commission_viability + w3·country_priority)
```
- 곱셈적 게이트: **공식 원천이 없거나 약관 위반이면 점수 0** → 편입 불가. 발견해도 신뢰 못 얻는 네트워크는 자동 탈락.

### 16.5 애그리게이터 우선 편입 정책 (Aggregator-First) — 현실 최적화
개별 머천트는 대부분 공식 API가 없어 §16.4 게이트에서 탈락한다. 따라서 **API를 제공하는 글로벌 애그리게이터를 최우선 편입 대상**으로 삼는다. 하나의 애그리게이터가 **수천 개 머천트·상품 피드를 단일 공식 API로 노출**하므로, 커버리지를 폭발적으로 늘리면서 "공식 API 원천" 원칙을 지킬 수 있다.

| 우선순위 | 애그리게이터 | 공식 API | 주요 권역 | trust |
|---------|------------|---------|----------|-------|
| 1 | **CJ Affiliate** | Link/Product REST + GraphQL | US·글로벌 | 0.95 |
| 1 | **Impact** | Partnership Cloud API | 글로벌 | 0.95 |
| 1 | **쿠팡 파트너스** | OpenAPI(HMAC) | KR | 0.90 |
| 2 | **Rakuten Advertising** | Advertising API | US·JP·글로벌 | 0.92 |
| 2 | **Awin** | Publisher API + Product Feeds | EU·글로벌 | 0.92 |
| 2 | **Amazon Associates** | PA-API v5 | US·글로벌 | 0.88 |
| 3 | **ClickBank** | Marketplace API | US·글로벌(디지털) | 0.85 |

- 이 목록은 `core.discovery_sources`에 **시드로 등록**(마이그레이션 `0005_discovery.sql`)되어, 발견 엔진이 최상위 신뢰도로 우선 스캔한다.
- 각 애그리게이터 안에서 §16.3 UCB 밴딧이 **(애그리게이터 × 카테고리)** arm 단위로 크롤 예산을 배분 → 어느 애그리게이터의 어느 카테고리가 실제로 돈이 되는지 학습.
- 커넥터 구축 순서도 이 우선순위를 따른다: 쿠팡(M1, KR 검증) → CJ·Impact(글로벌 대량) → Rakuten·Awin → Amazon·ClickBank.

---

## 17. 지속적 대규모 상품 수집 (Continuous Ingestion at Scale)

발견된 네트워크가 늘수록 상품 수집은 **끊임없이·대규모로** 돌아야 한다. 핵심은 "모두를 자주"가 아니라 **가치·변동성 기반 우선순위**다.

### 17.1 수집 계층 (Freshness SLA Tiering)
| 계층 | 대상 | 신선도 SLA | 근거 |
|------|------|-----------|------|
| **Hot** | 수익성 점수 상위·변동 잦은 오퍼 | 1~3시간 | 의사결정 직결 |
| **Warm** | 활성 카탈로그 | 12~24시간 | 커버리지 |
| **Cold** | 저성과·비활성 | 3~7일 | 비용 절감 |
| **Backfill** | 신규 네트워크 초기 적재 | 1회성 대량 | 온보딩 |

계층 배정은 **`change_tracking` 신호 + 수익성 점수**로 동적 재계산(오퍼가 뜨거워지면 Hot 승격).

### 17.2 확장 패턴
- **작업 샤딩:** (network × category) 파티션 단위 병렬 워커, Celery 큐 우선순위(Hot>Warm>Cold).
- **증분 우선:** since 커서/ETag/변경피드 활용, 풀스캔 최소화.
- **대규모 중복제거:** §5.4 엔티티 해소를 배치+스트리밍 혼합, GTIN 우선·트라이그램 블로킹(blocking key)으로 O(N²) 회피.
- **역압(backpressure):** 네트워크 레이트리밋 초과 시 큐 지연, 예산 초과 시 Cold 자동 강등.
- **자기치유:** 커넥터 연속 실패 시 서킷브레이커 → 네트워크 `is_active=false` 후 알림.

### 17.3 수집→분류 자동 트리거
새 오퍼가 Bronze에 들어오면 이벤트로 §5 정규화 → §18 분류 → §8 점수 파이프라인이 **자동 연쇄**된다(사람 개입 없이 "발견→분류→분석"이 흐른다).

---

## 18. 광고상품 분류 엔진 — 초지능 논리·수학 (Ad Product Classification)

**이 시스템의 지적 심장.** 구독자가 "무엇을 홍보할지"를 결정하도록, 모든 오퍼를 **다축(multi-axis)으로 분류하고 실행가능한 세그먼트로 배치**한다. 단순 카테고리 태깅이 아니라 **의미 임베딩 + 계층 분류 + 군집 발견 + 기회 사분면**의 4단 수학 파이프라인이다.

### 18.1 분류 축 (Classification Axes)
| 축 | 값 예시 | 산출 방식 |
|----|--------|----------|
| **범주(Category)** | 가전>주방가전 | 계층 택소노미(ltree) |
| **의도(Intent)** | 문제해결·충동·고관여 | 임베딩→룰/모델 |
| **가격대(Price Tier)** | 저가·중가·프리미엄 | 카테고리 내 분위수 |
| **수익모델(Monetization)** | CPS/CPC/CPA·수수료 티어 | 구조 필드 |
| **오디언스(Audience)** | 연령·성별·관심사 | 임베딩+룰(선택 AI) |
| **계절성(Seasonality)** | 상시·시즌·이벤트 | 시계열 위상 |
| **경쟁(Competition)** | 저·중·포화 | §18.5 |
| **기회 세그먼트(Segment)** | 금맥·신흥·안정·포화·회피 | §18.6 (파생) |

### 18.2 특징 벡터 (Feature Vector)
각 오퍼 `o`를 벡터로:
```
x(o) = [ α · ê(o) ;  β · ŝ(o) ]
  ê(o) : 상품명+설명 의미 임베딩(단위정규화, d차원)  ← (선택) AI 'embedding' 역량, 없으면 TF-IDF 폴백
  ŝ(o) : 구조 특징 표준화 벡터
         = z(log price_krw), 유효수수료율, norm(EPC), native_rank_pct,
           onehot(billing_type), onehot(country), seasonality_phase …
  α,β : 의미/구조 가중(기본 0.6/0.4, 튜닝)
```
> 임베딩은 **T3 보조**다. AI가 꺼져 있으면 TF-IDF/키워드 벡터로 폴백해 **코어 분류가 항상 동작**한다(정확도만 달라짐).

### 18.3 계층 분류 (Hierarchical, 확률·신뢰도)
택소노미 노드 `c`마다 학습 중심점(centroid) `μ_c`. 소프트맥스 확률로 분류하고 신뢰도를 정량화:
```
P(c | o) = softmax_c( cos(ê(o), μ_c) / T )          (T: 온도)
ĉ(o) = argmax_c P(c|o),   conf(o) = max_c P(c|o)
```
- `conf ≥ θ_high` → 자동 확정. `θ_low ≤ conf < θ_high` → 룰 보정. `conf < θ_low` → **§18.4 군집 발견 큐**로.
- 중심점은 승인된 라벨(운영자/룰)로 EMA 갱신: `μ_c ← (1−η)μ_c + η·ê(o)` → **분류기가 스스로 정교해진다**.

### 18.4 신흥 니치 발견 (Unsupervised, 자가확장 택소노미)
미분류·저신뢰 오퍼 임베딩에 **HDBSCAN** 군집화 → 기존 택소노미에 없는 **새 상품군**을 발견:
```
군집 g가  |g| ≥ N_min  ∧  응집도 coherence(g) ≥ ρ  이면
   → '새 카테고리 후보'를 ai_suggestions(target='taxonomy') 로 제안 → 승인 시 노드 신설
```
→ 새 네트워크·새 트렌드 상품이 들어와도 **택소노미가 스스로 성장**한다(§16 자가확장과 연동).

### 18.5 경쟁·포화 수학 (Competition & Saturation)
니치 `g`(동일/유사 상품군)에 대해:
```
공급 밀도    Supply(g) = 정규화( Σ_networks 오퍼수 )
지배도(HHI)  HHI(g) = Σ_i (share_i)²            (특정 네트워크/판매자 독점도, 0~1)
분산 엔트로피 H(g) = −Σ_i p_i·ln p_i            (가격/판매자 분포의 파편화)
경쟁지수     K(g) = clamp01( wa·Supply + wb·HHI − wc·H_norm )
```
- HHI 높음=소수 독점(진입 어려움), 엔트로피 높음=파편화(틈새 여지). K는 이를 결합.

### 18.6 기회 사분면 — 실행가능 세그먼트 (초지능 논리)
각 오퍼/니치를 3축 `Π(수익성)·D(수요)·K(경쟁)`로 요약하고 **기회 점수**로 사분면 분류:
```
Π = norm(EPC),  D = 수요지수(§8.2),  K = 경쟁지수(§18.5)   (모두 0~1, 카테고리 상대 분위수)
기회점수  O(o) = Π^a · D^b · (1−K)^c            (a,b,c=0.4/0.35/0.25, 곱셈적)
추세기울기 T(o) = 최근 D의 회귀 기울기(양수=상승)
```
| 세그먼트 | 조건 | 구독자 행동 |
|---------|------|-----------|
| 🟢 **금맥(Goldmine)** | Π↑ · D↑ · K↓ | 즉시 집중 |
| 🔵 **신흥(Rising)** | T>0 · K↓ · Π 중상 | 선점 진입 |
| 🟡 **안정(Cash Cow)** | Π↑ · D↑ · K↑ | 검증됨, 차별화 필요 |
| 🟠 **포화(Saturated)** | K↑ · 마진압축 | 회피/관망 |
| 🔴 **회피(Avoid)** | Π↓ ∨ D↓ | 제외 |
경계값은 **카테고리 내 로버스트 분위수**(예: 상위 25%↑, 하위 25%↓)로 자동 산출 → 카테고리마다 공정한 기준.

### 18.7 분류 저장

```sql
CREATE TABLE core.taxonomy_axes (          -- 다축 정의
  id SERIAL PRIMARY KEY, axis TEXT UNIQUE,  -- 'intent','price_tier','audience','seasonality'
  values JSONB);

CREATE TABLE analytics.product_classifications (
  offer_id BIGINT PRIMARY KEY REFERENCES core.offers(id),
  category_id INT REFERENCES core.categories(id),
  category_confidence NUMERIC(4,3),
  intent TEXT, price_tier TEXT, audience JSONB, seasonality TEXT,
  competition_index NUMERIC(6,3), opportunity_score NUMERIC(6,3),
  segment TEXT,                             -- goldmine|rising|cashcow|saturated|avoid
  trend_slope NUMERIC(8,5),
  embedding VECTOR(768),                    -- pgvector(선택, 유사상품·군집)
  method TEXT, classified_at TIMESTAMPTZ DEFAULT now());
CREATE INDEX idx_pc_segment ON analytics.product_classifications(segment);

CREATE TABLE analytics.niche_clusters (    -- 군집/니치 단위 집계
  id BIGSERIAL PRIMARY KEY, label TEXT, centroid VECTOR(768),
  size INT, coherence NUMERIC(4,3),
  supply NUMERIC(6,3), hhi NUMERIC(4,3), entropy NUMERIC(6,3),
  competition_index NUMERIC(6,3), avg_opportunity NUMERIC(6,3),
  status TEXT DEFAULT 'active', updated_at TIMESTAMPTZ DEFAULT now());
```

### 18.8 UI 반영 (분류의 소비)
- 오퍼 테이블/랭킹에 **세그먼트 뱃지**(🟢금맥 …) + 기회점수 컬럼.
- **기회 지도(Opportunity Map):** Π×K 산점도, 버블=D, 색=세그먼트 → 한눈에 "금맥 좌상단" 파악.
- **니치 탐색:** 군집(니치)별 공급·경쟁·평균기회로 "덜 붐비고 돈 되는 틈새" 드릴다운.
- 다축 필터(의도·가격대·오디언스·계절성)로 구독자가 자기 채널에 맞는 상품을 정밀 탐색.

---

## 19. SaaS 구독 · 멀티테넌시 아키텍처

수집·분류된 **글로벌 카탈로그는 전 구독자 공유(single shared catalog)** 이고, 구독자별로 다른 것은 **접근 권한·기능·사용량 한도**다. 즉 데이터는 테넌트별로 복제하지 않고, **엔타이틀먼트(entitlement)로 분기**한다 — 저장·운영 비용 최적.

### 19.1 테넌시 모델
- **공유 카탈로그:** `core.*`, `analytics.*`는 전역 1벌. 모든 테넌트가 같은 최신 데이터를 본다.
- **테넌트 격리 데이터:** 저장뷰·알림·API키·사용량은 `tenant_id`로 분리 + **PostgreSQL RLS**(Row-Level Security)로 강제.
- **플랜 게이팅:** 접근 가능한 네트워크 수·카테고리·신선도 계층·고급분석·내보내기·API 쿼터를 플랜으로 차등.

### 19.2 구독·과금 테이블

```sql
CREATE TABLE core.tenants (
  id BIGSERIAL PRIMARY KEY, name TEXT, owner_email TEXT,
  status TEXT DEFAULT 'active', created_at TIMESTAMPTZ DEFAULT now());

CREATE TABLE core.plans (
  code TEXT PRIMARY KEY,                    -- 'free','starter','pro','enterprise'
  display_name TEXT, price_monthly_usd NUMERIC(10,2),
  entitlements JSONB NOT NULL);             -- {max_alerts, api_rpm, export_rows, freshness:'hot',
                                            --  networks:['*'], advanced_analytics:true, seats:5 ...}
CREATE TABLE core.subscriptions (
  id BIGSERIAL PRIMARY KEY, tenant_id BIGINT REFERENCES core.tenants(id),
  plan_code TEXT REFERENCES core.plans(code),
  status TEXT,                              -- 'trialing'|'active'|'past_due'|'canceled'
  billing_provider TEXT DEFAULT 'stripe', external_sub_id TEXT,
  current_period_end TIMESTAMPTZ, seats INT DEFAULT 1);

CREATE TABLE core.usage_counters (         -- 사용량 계량(쿼터 집행)
  tenant_id BIGINT, metric TEXT,           -- 'api_calls','exports','alerts_active'
  period DATE, used BIGINT DEFAULT 0, quota BIGINT,
  PRIMARY KEY (tenant_id, metric, period));

CREATE TABLE core.users (
  id BIGSERIAL PRIMARY KEY, tenant_id BIGINT REFERENCES core.tenants(id),
  email TEXT UNIQUE, role TEXT,             -- 'owner'|'admin'|'analyst'|'viewer'
  created_at TIMESTAMPTZ DEFAULT now());
```

### 19.3 엔타이틀먼트 집행 (미들웨어)
```
요청 → [인증] → 테넌트·플랜 로드 → [엔타이틀먼트 게이트]
   ├─ 기능 접근?(advanced_analytics 등) → 없으면 402/업그레이드 유도
   ├─ 쿼터?(api_rpm/exports) → usage_counters 증가·초과 시 429
   └─ 데이터 범위?(networks/freshness) → 쿼리에 필터 주입(RLS+뷰)
```
- **신선도 차등:** Free는 24h 지연 스냅샷, Pro는 Hot(실시간에 가까움) → §17 계층과 자연 연동.
- **결제:** Stripe 웹훅으로 `subscriptions` 동기화, 연체 시 자동 강등. (결제·환불 등 금전 행위는 결제사 UI에서, 시스템은 상태만 반영.)

### 19.4 플랜 예시 (초안)
| 기능 | Free | Starter | Pro | Enterprise |
|------|------|---------|-----|-----------|
| 열람 네트워크 | 1 | 3 | 전체 | 전체 |
| 데이터 신선도 | 24h | 12h | Hot | Hot |
| 기회 세그먼트/지도 | 제한 | ✅ | ✅ 고급 | ✅ 고급 |
| 알림 | 3 | 20 | 무제한 | 무제한 |
| API 쿼터 | ✕ | 저 | 중 | 협의 |
| 내보내기(CSV/API) | ✕ | 제한 | ✅ | ✅+SLA |
| 시트(사용자) | 1 | 3 | 10 | 협의 |

### 19.5 멀티테넌트 운영 주의
- **공정 사용:** 테넌트별 레이트리밋으로 한 고객이 자원 독점 못 하게.
- **감사·격리:** RLS + `tenant_id` 필수, 크로스테넌트 조회 원천 차단, 감사로그.
- **데이터 신선도 vs 비용:** Hot 계층은 상위 플랜 수익으로 크롤 비용을 상쇄(단가 구조 정합).

---

## 20. 결론 (v3 설계 요지)

1. **거래성 데이터는 공식 API가 유일한 원천.** 자가확장 발견도 "후보 생성"까지만 하고, **공식 API/승인 피드를 갖춰야만 데이터 원천으로 승격**된다(§16). 발견 ≠ 신뢰.
2. **DB는 `products`(실물) ↔ `offers`(조건) 분리**, 모든 수치에 출처·신선도 부착. 전역 공유 카탈로그 + 테넌트 엔타이틀먼트(§19).
3. **자가확장은 UCB 밴딧으로 크롤 예산을 학습 배분**(§16.3) → 신규 네트워크를 자동 탐험하되 비용을 수학적으로 통제. 후보 스코어는 곱셈 게이트로 약관·원천 없는 곳을 0점 처리.
4. **광고상품 분류는 임베딩→계층분류(소프트맥스 신뢰도)→HDBSCAN 니치 발견→기회 사분면**의 4단 수학(§18). 랭킹은 `Π^a·D^b·(1−K)^c`의 곱셈적 기회점수로 금맥/신흥/안정/포화/회피를 자동 분류 — 구독자의 "무엇을 홍보할지"에 직답.
5. **택소노미가 스스로 성장**(§18.4)하고 **수집→분류→분석이 자동 연쇄**(§17.3)하여, 네트워크가 늘어도 사람 개입 없이 카탈로그가 확장·정제된다.
6. **SaaS는 공유 카탈로그 + 플랜별 엔타이틀먼트/신선도 차등**(§19)으로 저비용 멀티테넌시. 상위 플랜의 Hot 신선도 수익이 크롤 비용을 상쇄하는 정합 구조.
7. **AI는 여전히 플러그형 T3 보조** — 임베딩·발견·트렌드·크롤보조에 쓰이되 T1 불변·제안 격리·승인 게이트. 꺼도 코어(공식 API+룰+TF-IDF 폴백)가 완전히 동작한다.
8. **MVP는 쿠팡 단일 커넥터로 끝까지 완성** 후, §16 발견·§18 분류·§19 구독을 순차 확장.

---

### 부록 A. 멱등 UPSERT (변경분만 이력화)

```sql
INSERT INTO core.offers (network_id, external_product_id, title, price_amount, price_currency,
  billing_type, commission_kind, commission_rate, stock_status, native_rank, data_source, fetched_at, raw_ref)
VALUES (:net, :ext, :title, :price, :cur, :bt, :ck, :rate, :stock, :rank, 'official_api', :fetched, :raw)
ON CONFLICT (network_id, external_product_id) DO UPDATE SET
  price_amount = EXCLUDED.price_amount,
  commission_rate = EXCLUDED.commission_rate,
  stock_status = EXCLUDED.stock_status,
  native_rank = EXCLUDED.native_rank,
  fetched_at = EXCLUDED.fetched_at, updated_at = now()
WHERE core.offers.price_amount    IS DISTINCT FROM EXCLUDED.price_amount
   OR core.offers.commission_rate IS DISTINCT FROM EXCLUDED.commission_rate
   OR core.offers.stock_status    IS DISTINCT FROM EXCLUDED.stock_status;
-- 변경 시 트리거로 core.price_history 적재
```

### 부록 B. 수익성 점수 (의사코드)

```python
def profitability_score(offer, priors, fx, now) -> float:
    price_krw = to_krw(offer.price_amount, offer.price_currency, fx)
    e_sale = price_krw * offer.commission_rate if offer.commission_kind == "percent" \
             else to_krw(offer.commission_fixed_amount, offer.commission_currency, fx)
    cvr = priors.cvr(offer.category_id, price_krw)
    epc = e_sale * cvr
    demand = native_demand(offer)                 # 베스트랭크/SalesRank/gravity 정규화
    if ai_enabled("trend_signal"):
        demand = blend(demand, ai_trend(offer))   # 선택: AI 보강
    competition = clamp01(offer.competition_index or 0.5)
    epc_n = robust_quantile_norm(epc, offer.category_id)
    raw = (epc_n ** 0.5) * (demand ** 0.3) * ((1 - competition) ** 0.2)
    freshness = math.exp(-hours_since(offer.fetched_at, now) / TAU)
    return round(100 * raw * freshness, 3)
```

### 부록 C. AI 어댑터 등록 (의사코드)

```python
# 관리자 콘솔이 호출하는 서비스
def register_ai_provider(code, adapter, secret_ref, config, budget):
    provider = ai_providers.insert(code=code, adapter=adapter,
                 secret_ref=secret_ref, config=config,
                 monthly_budget_usd=budget, is_enabled=False)
    return provider   # 헬스체크 통과 후 관리자가 역량 토글로 활성화

def route_capability(capability, payload):
    # ai_capabilities에서 enabled=true를 priority 순으로, 폴백 지원
    for cap in enabled_capabilities(capability):          # priority ASC
        adapter = load_adapter(cap.provider.adapter)
        if adapter.health().ok and within_budget(cap.provider):
            sug = adapter.run(capability, payload)
            log_cost(cap.provider, adapter.unit_cost())
            return ai_suggestions.insert(provider_id=cap.provider_id,
                     capability=capability, suggestion=sug.data,
                     confidence=sug.confidence, status='pending')
    return None   # AI 없음 → 코어 규칙만으로 진행(정상)
```
